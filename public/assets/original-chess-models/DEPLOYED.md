# Runtime FBX integration

The browser renderer loads `chess.fbx` from this directory through `OriginalChessModelSet`.

Every imported piece is automatically centered, placed on the board surface, recolored for its side and uniformly scaled to fit inside one Cube Chess cell.
